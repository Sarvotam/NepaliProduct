<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shipping_address %>
    <%= form.text_area :shipping_address, id: :order_shipping_address %>
  </div>

  <div class="field">
    <%= form.label :billing_address %>
    <%= form.text_area :billing_address, id: :order_billing_address %>
  </div>

  <div class="field">
    <%= form.label :product_price %>
    <%= form.text_field :product_price, id: :order_product_price %>
  </div>

  <div class="field">
    <%= form.label :delivery_cost %>
    <%= form.text_field :delivery_cost, id: :order_delivery_cost %>
  </div>

  <div class="field">
    <%= form.label :discount %>
    <%= form.text_field :discount, id: :order_discount %>
  </div>

  <div class="field">
    <%= form.label :final_price %>
    <%= form.text_field :final_price, id: :order_final_price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :order_quantity %>
  </div>

  <div class="field">
    <%= form.label :order_description %>
    <%= form.text_area :order_description, id: :order_order_description %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :order_product_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :order_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
